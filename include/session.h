/*
Make a struct (structure)
A struct is like a variable but can hold different data types
instead of passing id and duration separately we pass them as a whole
the struct is named as Session
*/

typedef struct{
    int id;
    float duration;
} Session;